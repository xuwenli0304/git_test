1 it seems when i run test directly, the config file cannot be loaded.
but when i run from the starter class, everything seems to become normal.

2 we can use another set of config file with spring.profiles.active config and @Profile 
  annotation.